# Tasks — Spec 009: Robustez e Acessibilidade do Frontend (React + Vite)

- [ ] Criar componente `StatusBanner` exibindo `parcial`/`origem` a partir do `statusInfo` (R009-1)
- [ ] Renderizar `StatusBanner` em `App.jsx`, substituindo o uso apenas interno de `statusInfo` (R009-1)
- [ ] Implementar focus trap em `ObraDetailModal.jsx:7` (manual ou via `focus-trap-react`) (R009-2)
- [ ] Restaurar foco ao elemento anterior ao fechar o modal (R009-2)
- [ ] Refatorar `ObraCard.jsx:38` removendo `role="button"` com link aninhado; escolher elemento interativo único (R009-3)
- [ ] Substituir `Math.random()` em `ObraCard.jsx:5` por `numeroControlePNCP` (ou outro ID estável) como `key` (R009-4)
- [ ] Remover ou substituir o selo fixo de "WCAG 2.2 AA" em `AccessibilityFooter.jsx:21` por relatório real de auditoria (R009-5)
- [ ] Unificar cálculo de estatísticas do dashboard para usar `filteredObras` (R009-6)
- [ ] Mover filtro de UF para query param enviado ao backend endpoint `/obras` (R009-7)
- [ ] Implementar filtro `uf` no backend (`WHERE uf = :uf`) (R009-7)
- [ ] Criar módulo `src/api.js` centralizando `fetch`, com `res.ok`, `URLSearchParams` e `AbortController` (R009-8)
- [ ] Substituir chamadas `fetch` espalhadas pelo componente para usar `src/api.js` (R009-8)
- [ ] Auditar `App.css`, `hero.png`, SVGs de template, `SandboxApp.jsx`, `mockVendasData.js` quanto a uso real (grep de referências) (R009-9)
- [ ] Remover arquivos legados confirmados como não utilizados (R009-9)
- [ ] Rodar `npm run build` após limpeza e confirmar que nada quebrou (R009-9)
- [ ] Escrever teste de acessibilidade automatizado (axe-core/Testing Library) para o modal (R009-2)
- [ ] Escrever teste para estatísticas do dashboard com filtro + busca + ordenação combinados (R009-6)
- [ ] Escrever teste para cancelamento de requisição obsoleta em buscas rápidas sucessivas (R009-8)
