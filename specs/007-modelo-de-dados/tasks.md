# Tasks — Spec 007: Modelo de Dados e Persistência

- [ ] Alterar `data_publicacao` de `String` para `DateTime` em `backend/models/obra_model.py` (R007-1)
- [ ] Criar `Enum` de `Modalidade` com valores válidos e aplicar validação na entrada (R007-2)
- [ ] Alterar campo de valor financeiro de `Float` para `Numeric(14, 2)` em `backend/models/obra_model.py:20` (R007-3)
- [ ] Garantir que `cnpj` seja `String`, sem coerção/validação numérica, aceitando formato alfanumérico (R007-4)
- [ ] Instalar e configurar Alembic (`alembic init`, `env.py` apontando para `Base.metadata`) (R007-5)
- [ ] Gerar revisão inicial do Alembic capturando o schema atual (R007-5)
- [ ] Extrair `limpar_registros_antigos()` como função independente do fluxo de busca, em `backend/database.py` (R007-6)
- [ ] Chamar `limpar_registros_antigos()` a partir do `lifespan`/startup do FastAPI ou de scheduler (R007-6)
- [ ] Confirmar que `numeroControlePNCP`, `CNPJ`, `ano`, `sequencial` e payload original são persistidos sem transformação com perda (R007-7)
- [ ] Implementar `upsert_obras()` em lote usando `sqlite_insert(...).on_conflict_do_update(...)` (R007-8)
- [ ] Substituir loop de upsert item a item pela função em lote (R007-8)
- [ ] Substituir cache global (`backend/services/pncp_service.py:8`) por `cachetools.TTLCache` (ou Redis) com `maxsize`/`ttl` (R007-9)
- [ ] Auditar `requirements.txt` e remover dependências não importadas: `requests`, `pdfplumber`, `pypdf`, `asyncpg`, `apscheduler` (se confirmado não uso) (R007-10)
- [ ] Escrever teste unitário rejeitando `modalidade` inválida (R007-2)
- [ ] Escrever teste unitário validando precisão decimal de valores financeiros (R007-3)
- [ ] Escrever teste unitário aceitando CNPJ alfanumérico (R007-4)
- [ ] Escrever teste validando que limpeza roda mesmo com falha na última busca (R007-6)
- [ ] Escrever teste/benchmark comparando upsert em lote vs item a item (R007-8)
- [ ] Escrever teste validando expiração do cache por TTL (R007-9)
- [ ] Aplicar migração Alembic em cópia do banco com dados reais e validar integridade pós-migração (R007-5)
